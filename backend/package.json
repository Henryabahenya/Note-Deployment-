{
  "name": "back-practice",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
  "build:ui": "rm -rf dist && cd ../../Trials/frontend && npm run build && cp -r dist ../../back-practice/backend",
  "deploy:full": "npm run build:ui && git add . && git commit -m 'uibuild' && git push"

  
  },
  "author": "Henry Abahenya",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
